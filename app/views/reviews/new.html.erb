<h3> Let a new review for <%= @restaurant.name %> </h3>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: 0...5, selected: 0 %>
  <%= f.button :submit %>
<% end %>

<br> <%= link_to "Go back to home page", restaurants_path %>
